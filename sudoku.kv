<NumberSelector>:
    orientation: 'horizontal'

<MyLayout>:
    SudokuBoard:
        id: sudoku_board
        pos_hint: {'center': (0.5, 0.5)}
        size_hint: 0.9, 0.5
    
    NumberSelector:
        sudoku_board: sudoku_board
        sudoku_size: sudoku_board.sudoku_size
        size_hint: 0.9, 0.07 
        pos_hint: {'center': (0.5, 0.1)}

        # DEBUG COLOR
        # canvas:
        #     Color:
        #         rgba:  0.4, 0.4, 0.1, 1.0

        #     Rectangle:
        #         size: self.size
        #         pos: self.pos
